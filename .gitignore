results.txt
.env
.idea
__pycache__/
*.pyc
.DS_Store
node_modules/

# Local & Nested Workspaces
sdc-uav-prioritization-fse2027/
figure_refs/
kaggle-data/

# Media & Render cache
media/
video/

# Data
data/*.json
data/experiments/
# Large Kaggle dataset trees (keep local only; see scripts/download_kaggle_datasets.py)
data/kaggle/
# Zenodo dataset archives (local mirrors; DOI in paper/scripts)
data/zenodo/
# Third-party repos cloned for reference (nested .git dirs)
external/

# LaTeX/Beamer artifacts
*.aux
*.log
*.out
*.fls
*.fdb_latexmk
*.nav
*.snm
*.toc
*.pdf
*.gz
*.synctex(busy)
*.bbl
*.blg

# OS junk
Thumbs.db
.DS_Store

# local run config & logs (per-user, not for commit)
.kaggle-project.json
.kaggle-project.json.*
.kaggle-project*.json

# local run config & logs (per-user, not for commit)
*_log.log
*_ERROR.log
*_CANCEL.log
_*manifest*.json
